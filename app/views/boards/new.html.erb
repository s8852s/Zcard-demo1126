<h1>新增看板</h1>

<%= link_to "返回上一頁", :back %>

<%= form_for(@board) do |form| %>

<div class="field">
    <%= form.label :title, "看板名稱" %>
    <%= form.text_field :title %>
</div>


<%= form.submit %>
<% end %>